1. border collaspe

    <border-collapse>
    -separate:개별 셀을 독립적으로 분리하여 셀들 간의 간격 발생, 디폴드값
    -collapse: 셀들간의 간격을 없애서 경계를 통합.
    
    -셀 간 간격을 독립적eparate으로 유지할 것인지 붕괴collapse시켜서 없앨 것인지를 결정.
    각 셀은 별도의 독립된 보더라인 설정이 가능하며 각셀의 경계에는 일정한 간격이 존재하여 별도의 설정을 지정하지 않은 상태eparate로 보더라인을 지정하면 각 셀간의 보더라인을 따라서 일정한 간격이 발생되는 것을 확인 가능. 
    따라서 이러한 간격을 없애고 각 셀의 보더라인이 하나로 중첩되어 통합되도록 설정하려면 collapse값을 지정.
    단 이때 부모 요소가 된느 table태그의 bordercollapse 속성을 지정해야만 부모 자신을 포함한 하위 태그들에 대한 속상값이 적용된에 주의
    
    **테이블은 블럭 요소임에 주의 

2. inline, block, inline-block
    < inline, block, inline-block 특성 >
    - inline : 기본적으로 텍스트 속성을 가져 인라인 요소간 수평 나열이 가능하지만 크기 지정은 불가.
    - block : 높이는 내부 컨텐트에 의해 결정되지만 너비는 수평 영역 전체를 차지하는 특성으로 인해
    블럭요소간 수평 나열이 불가.
    단, 크기 지정은 가능한데 수평 영역 전체는 그대로 차지한 상태에서 컨텐트 영역을 제외한 나머지 부분은 마진 영역으로 채워지는 특성.
    - inline-block : 인라인 요소와 같이 컨텐트 영역만을 차지함으로써 인라인 특성을 가지는 요소간 수평 나열이 가능하면서도 블럭요소와 같이 크기 지정이 가능한 특성.
    ( 대표적 인라인 블럭요소 : img ) 또한 인라인 블럭 요소 내 다른 블럭요소의 포함이 가능.
    line-height 로 높이를 결정하였으므로 height 는 생략 가능.

3. display(none), visibility(hidden, visible)
    이미지에 대한 크기는 width 나 height 중 하나만 변경 해도 원본 이미지에 맞추어 자동으로 다른 속성의 비율이 조정.
    < display : none >
    - 해당 요소가 컨텐트 영역을 차지하지 않으면서 숨김.
    <visibility >
    - hidden : 해당 요소가 컨텐트 영역을 차지한 상태로 숨김.
    - visible : visibility 속성의 디폴트 값.

4. display(list-item), display(table, table-cell)
    < display : list-item >
    - 지정한 요소를 리스트와 같은 형태로 구현.
    지정 요소가 인라인 요소인 경우 리스트와 같은 블럭요소로 특성 변경.
    <display : table, table-cell >
    - table : 부모 요소에 지정하여 table 태그와 같은 역할 부여.
    - table-cell : 자식 요소에 지정하여 td 태그와 같은 역할 부여.

5. overflow
    < overflow >
    - 부모요소의 프레임 내에서 자식요소가 흘러 넘치는 경우에 대한 설정.
    < visible >
    - 부모요소에 대하여 자식요소가 흘러 넘쳐도 보이는 그대로 표시.
    overflow 속성 미설정 시의 디폴트 값.
    < hidden >
    - 부모요소에 대하여 자식요소가 흘러 넘쳐 표시되는 부분을 숨김.
    < white-space : nowrap >
    - 텍스트는 기본적으로 지정된 컨텐트 너비 영역에 둘러싸이는 특성(normal)으로 인해 오른쪽 끝 영역에 닿으면 자동으로 개행되는 특성을 가지는데, 이러한 너비를 둘러싸는 디폴트 속성을 해제하는 값이 nowrap.
    < overflow-x : hidden >
    - 부모요소에 대하여 자식요소가 수평으로 흘러 넘쳐 표시되는 부분을 숨김
    < overflow-y : hidden >
    - 부모요소에 대하여 자식요소가 수직으로 흘러 넘쳐 표시되는 부분을 숨김.
    < scroll >
    - 자식 요소가 부모의 수평 또는 수직 영역을 벗어나 흘러 넘치는 경우, 흘러 넘치는 방향에 상관없이 수평과 수직 모두에 대하여 스크롤 바 생성.
    < auto >
    - 자식 요소가 부모의 수평 또는 수직 영역을 벗어나 흘러 넘치는 경우, 흘러 넘치는 방향에 대해서만 스크롤 바 생성. ( ex : 약관 )

6. 예제

7.