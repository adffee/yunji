1. HTML
    전체파일 참고
2. 태그 작성 규칙과 엔터티코드
    < 태그 작성 규칙 >
    1> 모든 태그는 소문자로 작성하는 것이 원칙.
    2> 연속된 white-space character(space, tab, enter)는 하나만 인정.
    3> 웹 페이지에 표시하려는 특수문자(<, > ," , &, 연속된 공백 등)는 "&" 와 ";" 사이에 예약어를 입력하여 표시.
    ( 4-HTML5-2_특수문자 - 엑셀 파일 참조,    https://entitycode.com  - 참조 )
    4> 웹 표준은 HTML, CSS, Javascript 가 모두 분리되어 작성되어야함이 원칙.
    < pre 태그 >    
    - "미리 형성된" 의미로 의역하자면
    - White-space character(space, tab, enter)를 입력한 그대로 표시.
    단, 그 외 특수문자는 entitycode 형식 그대로 표현해야함에 주의.
3. heading 태그
    < 헤딩(Heading) 태그 >
    - 웹페이지 표제를 표시하기 위한 태그로 1 ~ 6 까지의 태그가 존재하며 h1 태그의 경우 문서의 가장 상단에 배치하는 것을 권장. 
    " 1 > 2 > 3... > 6 " 의 관계로 폰트 크기가 결정되며 자동 개행되는 특징.
    헤딩 태그는 단순히 글자 크기를 나타내는 것뿐만이 아니라 검색 엔진이 웹 페이지의 컨텐츠 중요도를 인식하는 매개체로 활용. ( 블럭 요소에 해당 )
    따라서 동일 레벨의 헤딩 태그가 중복되는 것은 문제가 되지 않지만, 헤딩 태그의 레벨을 생략하여 건너뛰거나 레벨의 순서를 역순으로 배치하는 것은 권장되지 않음.
4. text와 문단 관련 태그
    일반적으로 블럭 레벨 요소는 다른 블럭 레벨 요소를 포함할 수 있지만
    p 태그의 경우 인라인 요소만 포함 가능함에 주의.
    < hr 태그 > - Horizontal Rules
    - 수평선을 적용하는 태그로 일반적으로 끝태그가 없는 형식을 취함.
    < br 태그 > - Line Break
    - 줄 바꿈(개행)을 의미하는 태그.
    < Superscript 와 Subscript >
    - 각각 윗 첨자와 아랫 첨자를 표현하느 태그로써 인라인 레벨 요소.
    < abbr 태그 >  - Abbreviation
    - 약어에 대한 full name 을 title 속성을 통해 명시함으로써 검색지수를 높일 수 있는 태그. 따라서 abbr 태그에 포함된 컨텐츠 뿐만아니라, title 속성에 명시된 개별 단어로도 검색이 가능.
    title 속성은 컨텐트에 대한 툴팁(tooltip) 기능을 포함하며 dot 형식의 underline 형태로 표시.
    < dfn 태그 > - Definition
    - abbr 태그와 마찬가지로 title 속성을 통해 검색지수를 높일 수 있는 태그로써, 문서 내에서 최초로 사용되는 용어에 대한 정의를 할 때 사용.
    단, dfn 태그의 부모 요소에는 해당 용어에 대한 정의나 설명이 반드시 포함되어야 함에 주의.
    abbr 태그와 마찬가지로 컨텐트에 대한 툴팁(tooltip) 기능을 포함하며 이탤릭 형태로 표시.
    < blockquote 태그 >
    - 독립적인 블럭 레벨 요소 형태의 인용문을 작성할 때 사용되며 기본적으로 블럭의 좌/우가 들여쓰기 형태로 표시.
    cite 속성을 통해 인용문에 대한 출처를 명시.
    < q 태그 >  - quote
    - 짧은 형식의 인용문을 작성할 때 사용되는 인라인 레벨 요소이며 쌍따옴표로 감싸진 형태로 웹 페이지에 표시.
    < address 태그 >
    - 주소나 연락처 상호등을 표시하기 위한 태그로 이탤릭체가 디폴트로 설정. 
    굳이 address 태그를 별도로 사용하는 것은 검색엔진에 대한 검색 효율성을 보장하기 위함.
5. 섹셔닝
    span태그 영역을 그룹화하기 위한 인라인 레벨 요소로써 다른 인라인 레벨 요소들의 컨텐츠를 그룹화
    div 블럭레벨 그룹화
6. 예제