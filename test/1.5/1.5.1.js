'use strict';

const person = {
    name1 : 'romii',
    age : 29,
};

/**
 * 위의 person 객체에 name 프로퍼티와 age 프로퍼티를 추가하고 console.log로 출력하라.
 * 출력 방식, 텍스트의 내용은 자유롭게 구성해도 상관 없다.
 */

 console.log(`이름: ${person.name1} 나이: ${person.age}`);